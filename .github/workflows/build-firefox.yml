name: Check Firefox Builder

on:
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - name: Check builder
        run: |
          echo "GitHub builder is working."
          echo "Available storage:"
          df -h /
          echo "Checking Firefox source:"
          git ls-remote https://github.com/mozilla-firefox/firefox.git HEAD
